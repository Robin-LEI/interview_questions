**变量提升**

通过var声明的变量，在定义语句之前就可以访问到，但是并没有赋值。



**函数声明提升**

通过function声明的函数，在之前就可以直接调用



**变量提升和函数提升是如何产生的？**

JS引擎在执行js代码的时候，会先进行预编译，此时引擎会优先解析var声明的变量和function声明的函数。预编译完成之后，才会从上到下依次执行。在预编译function声明的函数的时候，会把函数整体定义提前，这就解释了为什么函数可以先调用在声明。